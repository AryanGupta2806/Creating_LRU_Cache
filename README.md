# Creating_LRU_Cache
This project implements an LRU (Least Recently Used) Cache in C++ using a combination of a Doubly Linked List and an unordered_map. The cache efficiently stores key-value pairs while maintaining a fixed capacity. When the cache becomes full, it automatically removes the least recently used item before inserting a new one.
